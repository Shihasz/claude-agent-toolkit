# Claude Agent Toolkit

A production-grade, multi-tool AI agent built on the Claude API —
tool use, structured outputs, and a hardened tool framework, exposed
through both a CLI and a REST API.

Work in progress — see commit history for the build-up.
